services:
  php-api:
    build: .
    container_name: mediathekarr-api
    ports:
      - "8090:80"
    volumes:
      - ./../api:/var/www/html
    environment:
      - APACHE_RUN_USER=www-data
      - APACHE_RUN_GROUP=www-data
    restart: unless-stopped
